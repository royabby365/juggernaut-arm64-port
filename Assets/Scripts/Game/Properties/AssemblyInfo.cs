// AssemblyVersion intentionally omitted: both Properties/AssemblyInfo.cs files
// compile into the same Assembly-CSharp, so a second [assembly: AssemblyVersion]
// here would be CS0579 (duplicate attribute). Version comes from
// PlayerSettings.bundleVersion in BuildScript/BuildPlayer instead.